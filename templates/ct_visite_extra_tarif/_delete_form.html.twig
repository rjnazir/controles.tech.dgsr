<form method="post" action="{{ path('vet_delete', {'id': ct_visite_extra_tarif.id}) }}" onsubmit="return confirm('Est-vous sûr de vouloir supprimer cet enregistrement?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ct_visite_extra_tarif.id) }}">
    <button class="btn btn-sm bg-gradient-danger text-white btn-icon-split ml-2 mr-2">
        <span class="icon text-white">
            <i class="fa fa-trash"></i>
        </span>
        <span class="text">Supprimer tarif visite extra</span>
    </button>
</form>
