{% extends 'base.html.twig' %}

{% block title %}CtExpressionBesoin{% endblock %}

{% block body %}
    <h1>CtExpressionBesoin</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ct_expression_besoin.id }}</td>
            </tr>
            <tr>
                <th>EdbNumero</th>
                <td>{{ ct_expression_besoin.edbNumero }}</td>
            </tr>
            <tr>
                <th>EdbDateEdit</th>
                <td>{{ ct_expression_besoin.edbDateEdit ? ct_expression_besoin.edbDateEdit|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>EdbQteDemander</th>
                <td>{{ ct_expression_besoin.edbQteDemander }}</td>
            </tr>
            <tr>
                <th>EdbCreatedAt</th>
                <td>{{ ct_expression_besoin.edbCreatedAt ? ct_expression_besoin.edbCreatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>EdbUpdatedAt</th>
                <td>{{ ct_expression_besoin.edbUpdatedAt ? ct_expression_besoin.edbUpdatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ct_expression_besoin_index') }}">back to list</a>

    <a href="{{ path('app_ct_expression_besoin_edit', {'id': ct_expression_besoin.id}) }}">edit</a>

    {{ include('ct_expression_besoin/_delete_form.html.twig') }}
{% endblock %}
